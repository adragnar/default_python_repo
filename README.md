To initialize repo:

1) Install uv
2) Make sure the right version of python is installed with uv python install <version>
3) Initialize dir with uv init --python <version>
4) Check that the right version of python is installed py contents of .python-version
5) Install needed dependencies with uv add <lib>
6) Sync project with uv sync
